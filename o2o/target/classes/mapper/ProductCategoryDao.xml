<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.o2o.dao.ProductCategoryDao">
	<!-- 目的：为dao接口方法提供sql语句配置 -->
	<select id="queryProductCategoryList" resultType="com.imooc.o2o.entity.ProductCategory"
		parameterType="Long">
		<!-- 具体的sql语句 -->
		select
		product_category_id,
		product_category_name,
		priority,
		create_time,
		shop_id
		from
		tb_product_category
		where
		shop_id=#{shopId}
		order by
		priority
		DESC
	</select>
	<!-- 批量增加商品类别 -->
	<insert id="batchInsertProductCategory" parameterType="java.util.List">
		insert into
		tb_product_category(product_category_name,priority,create_time,shop_id)
		values
		<!-- 用foreach进行遍历，参数分别表示为：集合的类型，存取集合中元素的变量，计数器，分隔符 -->
		<foreach collection="list" item="productCategory" index="index"
			separator=",">
			(
			#{productCategory.productCategoryName},
			#{productCategory.priority},
			#{productCategory.createTime},
			#{productCategory.shopId}
			)
		</foreach>
	</insert>
	<!-- 传入shop_id是为了防止别的店铺的操作，造成本店铺的影响 -->
	<delete id="deleteProductCategory">
		delete from
		tb_product_category
		where
		product_category_id = #{productCategoryId}
		and shop_id = #{shopId}
	</delete>
</mapper>